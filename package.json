{
  "name": "blader-stack",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun run dev:server\" \"bun run dev:admin\"",
    "dev:server": "bun --watch apps/server/src/index.ts",
    "dev:admin": "cd apps/admin && bun run dev",
    "build:server": "cd apps/server && bun build --compile --target bun --outfile server src/index.ts",
    "setup-ui-app": "bun scripts/setup-ui-app.mjs"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.2.1",
    "typescript": "^5"
  }
}
